package src.main.BasicQuestions;

public class ProducerConsumerProblem {
    static void main() {

        //Producer Consumer problem is a multithreading problem, where one thread(producer) creates the
        // data and another thread(consumer) uses this data. A shared buffer is used between them.
        //When the buffer gets full producer waits & when the buffer gets empty consumer waits
        // This synchronization between two thread helps prevent data loss and conflicts between
        // threads while working on same shared resource.
    }
}
